给定一个字符串 s ，请你找出其中不含有重复字符的 最长子串 的长度。

示例 1:
输入: s = "abcabcbb"
输出: 3
解释: 因为无重复字符的最长子串是 "abc"，所以其长度为 3。

示例 2:
输入: s = "bbbbb"
输出: 1
解释: 因为无重复字符的最长子串是 "b"，所以其长度为 1。

示例 3:
输入: s = "pwwkew"
输出: 3
解释: 因为无重复字符的最长子串是 "wke"，所以其长度为 3。
     请注意，你的答案必须是 子串 的长度，"pwke" 是一个子序列，不是子串。

示例 4:
输入: s = ""
输出: 0

1. 字符串长度为0 --> 返回0
2. 字符串长度为1 --> 返回1
3. 字符串长度为其他：
    1. 给出一个指针，用来向后循环依次找不重复字符
    2. 给一个变量用来记录最长长度子串
    3. 用一个map来存储字符串的值，K为内容，V为索引位置。
    4. 当出现重复元素后，找到之前的重复元素，以之前的重复元素的索引的下一个位置开始进行下一次遍历
    5. 重复以上步骤，当再次重复后，判断剩下的元素长度是否小于最长子串长度，如果小于，则直接break。